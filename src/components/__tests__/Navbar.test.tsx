import { describe, it } from "vitest";

describe("Navbar", () => {
  it.skip("should render the Wrathfinder logo", () => {});
  it.skip("should render the Wrathfinder wordmark", () => {});
  it.skip("should render the navigation bar", () => {});
});
